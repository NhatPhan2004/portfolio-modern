# TODO - Fix navbar sync

- [x] Step 1: Remove erroneous `useNavbar()` usage from `src/App.jsx` (only `Navbar.jsx` should call the hook)

- [ ] Step 2: (Optional) Improve `useNavbar` active-section calculation using IntersectionObserver (not done yet)

- [x] Step 3: Run dev server / verify scroll + mobile menu behavior
